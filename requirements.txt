# Strands Agents SDK — Agent, @tool, BedrockModel, FileSessionManager
strands-agents>=1.7.0

# A2A SDK — Agent-to-Agent protocol
a2a-sdk<1.0.0

# Model Context Protocol (MCP) SDK & Open-Source MCP Servers
mcp>=2.1.1
duckduckgo-mcp-server>=0.7.0

# Loads KEY=VALUE pairs from .env into os.environ
python-dotenv>=1.0.0

# FastAPI — web framework for the /chat endpoint
fastapi>=0.111.0

# Uvicorn — ASGI server to run FastAPI
uvicorn[standard]>=0.29.0

# Web utilities
beautifulsoup4>=4.12.0
httpx>=0.28.0

# OpenTelemetry Distributed Tracing & Jaeger Export
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0
